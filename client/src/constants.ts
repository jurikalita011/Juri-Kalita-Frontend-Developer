export type Products = {
  img: string;
  title: string;
  para: string;
};

export type Token = {
  img: string;
  tokenName: string;
  symbol: string;
  decimals: number;
  marketcap: number;
  chain: string;
};
